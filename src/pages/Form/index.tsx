import UserForm from './UserForm';
import styled from './styled.module.scss';
const Form = () => {
	return (
		<div className={styled.test}>
			<UserForm />
			<h4 className={styled.teste3}>Tiago</h4>
		</div>
	);
};

export default Form;
